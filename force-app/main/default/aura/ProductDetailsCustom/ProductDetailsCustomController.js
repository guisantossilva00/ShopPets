({
	initialize: function(component, event, helper) {
        helper.loadProductDetails(component, event, helper);
        helper.loadProductDescription(component, event, helper);        
    }        
})